.DS_Store
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# AI-context tier (workspace convention)
AGENTS.md
.agents.md
CLAUDE.md

# macOS AppleDouble resource forks
._*

# Python build artifacts
*.egg-info/

# Internal drafts (working files, not for the public spec repo)
*-DRAFT.md

# Vendored reference snapshot for the honest-framework reference test: a local,
# Local Paper A v2 run scratch (results, logs, summary). An experimental arm, so it
# lives under research/arms/ rather than inside the implementation it happens to drive.
research/arms/paper-a-v2/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Agent worktrees. Created by background agents working in isolation; each is a
# checkout of this same repository and belongs to no commit.
.claude/worktrees/

# Output of scripts/cardinality_distribution.py, which defaults to the working directory.
cardinality-distribution.json
